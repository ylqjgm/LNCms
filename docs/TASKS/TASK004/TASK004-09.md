# TASK004-09: 实现UserGroupRepository接口和实现

## 任务信息

**任务编号**: TASK004-09  
**父任务**: TASK004  
**任务名称**: 实现UserGroupRepository接口和实现  
**版本信息**: v1.1.0  
**当前状态**: 计划中  
**创建时间**: 2026-01-03 14:54:20 CST

## 任务描述

实现UserGroupRepository接口，提供用户组的CRUD操作。包括：1) 定义UserGroupRepository接口；2) 实现用户组的创建、查询、更新、删除方法（Create、GetByID、GetAll、Update、Delete）；3) 实现数据库操作逻辑。所有代码必须遵循Golang编码规范，错误消息使用中文。

## 依赖任务

- TASK004-08: 创建UserGroup模型

## 实现指南

### 1. 定义UserGroupRepository接口

在 `internal/pkg/repository/user_group_repository.go` 文件中定义接口：

```go
package repository

import (
    "context"
    "yourproject/internal/pkg/model"
)

// UserGroupRepository 用户组仓储接口
type UserGroupRepository interface {
    // GetByID 根据ID获取用户组
    GetByID(ctx context.Context, id int64) (*model.UserGroup, error)
    
    // GetAll 获取所有用户组
    GetAll(ctx context.Context) ([]*model.UserGroup, error)
    
    // Create 创建用户组
    Create(ctx context.Context, userGroup *model.UserGroup) error
    
    // Update 更新用户组
    Update(ctx context.Context, userGroup *model.UserGroup) error
    
    // Delete 删除用户组（软删除）
    Delete(ctx context.Context, id int64) error
    
    // GetByGroupName 根据用户组名称获取用户组
    GetByGroupName(ctx context.Context, groupName string) (*model.UserGroup, error)
}
```

### 2. 实现userGroupRepository结构体

在同一个文件中实现用户组仓储：

```go
import (
    "context"
    "fmt"
    "gorm.io/gorm"
    "yourproject/internal/pkg/model"
)

// userGroupRepository 用户组仓储实现
type userGroupRepository struct {
    db *gorm.DB
}

// NewUserGroupRepository 创建用户组仓储实例
func NewUserGroupRepository(db *gorm.DB) UserGroupRepository {
    return &userGroupRepository{db: db}
}
```

### 3. 实现GetByID方法

实现根据ID获取用户组的方法：

```go
// GetByID 根据ID获取用户组
func (r *userGroupRepository) GetByID(ctx context.Context, id int64) (*model.UserGroup, error) {
    var userGroup model.UserGroup
    err := r.db.WithContext(ctx).
        Where("id = ?", id).
        First(&userGroup).Error
    if err != nil {
        if err == gorm.ErrRecordNotFound {
            return nil, fmt.Errorf("用户组不存在")
        }
        return nil, fmt.Errorf("查询用户组失败: %w", err)
    }
    return &userGroup, nil
}
```

### 4. 实现GetAll方法

实现获取所有用户组的方法：

```go
// GetAll 获取所有用户组
func (r *userGroupRepository) GetAll(ctx context.Context) ([]*model.UserGroup, error) {
    var userGroups []*model.UserGroup
    err := r.db.WithContext(ctx).
        Order("created_at DESC").
        Find(&userGroups).Error
    if err != nil {
        return nil, fmt.Errorf("查询用户组列表失败: %w", err)
    }
    return userGroups, nil
}
```

### 5. 实现Create方法

实现创建用户组的方法：

```go
// Create 创建用户组
func (r *userGroupRepository) Create(ctx context.Context, userGroup *model.UserGroup) error {
    if err := r.db.WithContext(ctx).Create(userGroup).Error; err != nil {
        return fmt.Errorf("创建用户组失败: %w", err)
    }
    return nil
}
```

### 6. 实现Update方法

实现更新用户组的方法：

```go
// Update 更新用户组
func (r *userGroupRepository) Update(ctx context.Context, userGroup *model.UserGroup) error {
    result := r.db.WithContext(ctx).
        Model(&model.UserGroup{}).
        Where("id = ?", userGroup.ID).
        Updates(userGroup)
    if result.Error != nil {
        return fmt.Errorf("更新用户组失败: %w", result.Error)
    }
    if result.RowsAffected == 0 {
        return fmt.Errorf("用户组不存在")
    }
    return nil
}
```

### 7. 实现Delete方法

实现删除用户组的方法（软删除）：

```go
// Delete 删除用户组（软删除）
func (r *userGroupRepository) Delete(ctx context.Context, id int64) error {
    result := r.db.WithContext(ctx).
        Where("id = ?", id).
        Delete(&model.UserGroup{})
    if result.Error != nil {
        return fmt.Errorf("删除用户组失败: %w", result.Error)
    }
    if result.RowsAffected == 0 {
        return fmt.Errorf("用户组不存在")
    }
    return nil
}
```

### 8. 实现GetByGroupName方法

实现根据用户组名称获取用户组的方法：

```go
// GetByGroupName 根据用户组名称获取用户组
func (r *userGroupRepository) GetByGroupName(ctx context.Context, groupName string) (*model.UserGroup, error) {
    var userGroup model.UserGroup
    err := r.db.WithContext(ctx).
        Where("group_name = ?", groupName).
        First(&userGroup).Error
    if err != nil {
        if err == gorm.ErrRecordNotFound {
            return nil, fmt.Errorf("用户组不存在")
        }
        return nil, fmt.Errorf("查询用户组失败: %w", err)
    }
    return &userGroup, nil
}
```

### 9. 代码规范要求

- 所有错误消息使用中文
- 使用context.Context传递上下文
- 使用GORM的WithContext方法
- 处理GORM错误，包括RecordNotFound错误
- 在Update和Delete方法中检查RowsAffected
- 所有方法都有清晰的注释（使用中文）

## 验收标准

1. **接口定义完整**
   - UserGroupRepository接口包含所有必需的方法：GetByID、GetAll、Create、Update、Delete、GetByGroupName
   - 方法签名正确，参数和返回值类型正确
   - 接口注释完整，使用中文

2. **实现完整**
   - userGroupRepository结构体正确实现UserGroupRepository接口
   - 所有接口方法都有对应的实现
   - 使用GORM进行数据库操作
   - 使用context.Context传递上下文

3. **错误处理正确**
   - 所有错误都使用中文错误消息
   - 正确处理GORM的RecordNotFound错误
   - Update和Delete方法检查RowsAffected
   - 错误消息清晰，包含上下文信息

4. **代码规范符合要求**
   - 代码通过golangci-lint检查，无错误和警告
   - 代码注释完整，使用中文
   - 遵循Golang编码规范
   - 遵循项目代码组织结构

5. **功能正确性**
   - GetByID方法正确查询用户组
   - GetAll方法正确返回所有用户组列表
   - Create方法正确创建用户组
   - Update方法正确更新用户组
   - Delete方法正确删除用户组（软删除）
   - GetByGroupName方法正确根据名称查询用户组

## 相关文件

- `internal/pkg/repository/user_group_repository.go` - 用户组仓储接口和实现
- `internal/pkg/model/user_group.go` - 用户组模型定义
- `docs/TASKS/TASK004/TASK004-08.md` - 创建UserGroup模型任务文档
- `.cursor/rules/golang.mdc` - Golang编码规范
- `docs/TASKS/TASK004/TASK004.md` - 主任务文档

## Git 提交信息任务

任务完成后，需要生成 Git 提交信息。请根据实际修改内容生成合适的提交信息。

---

**文档版本**: 1.1.0  
**最后更新**: 2026-01-03 14:54:20 CST

