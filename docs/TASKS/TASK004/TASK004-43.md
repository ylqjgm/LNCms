# TASK004-43: 实现UserGroupPermissionRepository接口和实现

## 任务信息

**任务编号**: TASK004-43  
**父任务**: TASK004  
**任务名称**: 实现UserGroupPermissionRepository接口和实现  
**版本信息**: v1.1.0  
**当前状态**: 计划中  
**创建时间**: 2026-01-03 15:00:00 CST

## 任务描述

实现UserGroupPermissionRepository接口，提供用户组权限关联的CRUD操作。包括：1) 定义UserGroupPermissionRepository接口；2) 实现用户组权限关联的创建、查询、删除方法（Create、GetByUserGroupID、GetByPermissionID、Delete）；3) 实现批量操作方法（BatchCreate、BatchDelete）。所有代码必须遵循Golang编码规范，错误消息使用中文。

## 依赖任务

- TASK004-40: 创建UserGroupPermission模型

## 实现指南

### 1. 参考数据库设计

阅读 `docs/TASKS/TASK002/TASK002-09.md` 文档，了解用户组权限关联表的数据库设计：
- 表名：`user_group_permissions`
- 字段：id、user_group_id、permission_id、created_at
- 外键：user_group_id关联user_groups表，permission_id关联permissions表
- 唯一约束：user_group_id和permission_id的组合唯一

### 2. 定义UserGroupPermissionRepository接口

在 `internal/pkg/repository/user_group_permission_repository.go` 文件中定义接口：

```go
package repository

import (
    "context"
    "yourproject/internal/pkg/model"
)

// UserGroupPermissionRepository 用户组权限关联仓储接口
type UserGroupPermissionRepository interface {
    // Create 创建用户组权限关联
    Create(ctx context.Context, userGroupPermission *model.UserGroupPermission) error
    
    // GetByUserGroupID 根据用户组ID获取权限列表
    GetByUserGroupID(ctx context.Context, userGroupID int64) ([]*model.UserGroupPermission, error)
    
    // GetByPermissionID 根据权限ID获取用户组列表
    GetByPermissionID(ctx context.Context, permissionID int64) ([]*model.UserGroupPermission, error)
    
    // GetByUserGroupIDAndPermissionID 根据用户组ID和权限ID获取关联
    GetByUserGroupIDAndPermissionID(ctx context.Context, userGroupID, permissionID int64) (*model.UserGroupPermission, error)
    
    // Delete 删除用户组权限关联
    Delete(ctx context.Context, id int64) error
    
    // DeleteByUserGroupIDAndPermissionID 根据用户组ID和权限ID删除关联
    DeleteByUserGroupIDAndPermissionID(ctx context.Context, userGroupID, permissionID int64) error
    
    // BatchCreate 批量创建用户组权限关联
    BatchCreate(ctx context.Context, userGroupPermissions []*model.UserGroupPermission) error
    
    // BatchDelete 批量删除用户组权限关联
    BatchDelete(ctx context.Context, userGroupID int64, permissionIDs []int64) error
}
```

### 3. 实现userGroupPermissionRepository结构体

在同一个文件中实现用户组权限关联仓储：

```go
import (
    "context"
    "fmt"
    "gorm.io/gorm"
    "yourproject/internal/pkg/model"
)

// userGroupPermissionRepository 用户组权限关联仓储实现
type userGroupPermissionRepository struct {
    db *gorm.DB
}

// NewUserGroupPermissionRepository 创建用户组权限关联仓储实例
func NewUserGroupPermissionRepository(db *gorm.DB) UserGroupPermissionRepository {
    return &userGroupPermissionRepository{db: db}
}
```

### 4. 实现Create方法

实现创建用户组权限关联的方法：

```go
// Create 创建用户组权限关联
func (r *userGroupPermissionRepository) Create(ctx context.Context, userGroupPermission *model.UserGroupPermission) error {
    if err := r.db.WithContext(ctx).Create(userGroupPermission).Error; err != nil {
        return fmt.Errorf("创建用户组权限关联失败: %w", err)
    }
    return nil
}
```

### 5. 实现GetByUserGroupID方法

实现根据用户组ID获取权限列表的方法：

```go
// GetByUserGroupID 根据用户组ID获取权限列表
func (r *userGroupPermissionRepository) GetByUserGroupID(ctx context.Context, userGroupID int64) ([]*model.UserGroupPermission, error) {
    var userGroupPermissions []*model.UserGroupPermission
    err := r.db.WithContext(ctx).
        Where("user_group_id = ?", userGroupID).
        Find(&userGroupPermissions).Error
    if err != nil {
        return nil, fmt.Errorf("查询用户组权限关联失败: %w", err)
    }
    return userGroupPermissions, nil
}
```

### 6. 实现GetByPermissionID方法

实现根据权限ID获取用户组列表的方法：

```go
// GetByPermissionID 根据权限ID获取用户组列表
func (r *userGroupPermissionRepository) GetByPermissionID(ctx context.Context, permissionID int64) ([]*model.UserGroupPermission, error) {
    var userGroupPermissions []*model.UserGroupPermission
    err := r.db.WithContext(ctx).
        Where("permission_id = ?", permissionID).
        Find(&userGroupPermissions).Error
    if err != nil {
        return nil, fmt.Errorf("查询用户组权限关联失败: %w", err)
    }
    return userGroupPermissions, nil
}
```

### 7. 实现GetByUserGroupIDAndPermissionID方法

实现根据用户组ID和权限ID获取关联的方法：

```go
// GetByUserGroupIDAndPermissionID 根据用户组ID和权限ID获取关联
func (r *userGroupPermissionRepository) GetByUserGroupIDAndPermissionID(ctx context.Context, userGroupID, permissionID int64) (*model.UserGroupPermission, error) {
    var userGroupPermission model.UserGroupPermission
    err := r.db.WithContext(ctx).
        Where("user_group_id = ? AND permission_id = ?", userGroupID, permissionID).
        First(&userGroupPermission).Error
    if err != nil {
        if err == gorm.ErrRecordNotFound {
            return nil, fmt.Errorf("用户组权限关联不存在")
        }
        return nil, fmt.Errorf("查询用户组权限关联失败: %w", err)
    }
    return &userGroupPermission, nil
}
```

### 8. 实现Delete方法

实现删除用户组权限关联的方法：

```go
// Delete 删除用户组权限关联
func (r *userGroupPermissionRepository) Delete(ctx context.Context, id int64) error {
    result := r.db.WithContext(ctx).
        Where("id = ?", id).
        Delete(&model.UserGroupPermission{})
    if result.Error != nil {
        return fmt.Errorf("删除用户组权限关联失败: %w", result.Error)
    }
    if result.RowsAffected == 0 {
        return fmt.Errorf("用户组权限关联不存在")
    }
    return nil
}
```

### 9. 实现DeleteByUserGroupIDAndPermissionID方法

实现根据用户组ID和权限ID删除关联的方法：

```go
// DeleteByUserGroupIDAndPermissionID 根据用户组ID和权限ID删除关联
func (r *userGroupPermissionRepository) DeleteByUserGroupIDAndPermissionID(ctx context.Context, userGroupID, permissionID int64) error {
    result := r.db.WithContext(ctx).
        Where("user_group_id = ? AND permission_id = ?", userGroupID, permissionID).
        Delete(&model.UserGroupPermission{})
    if result.Error != nil {
        return fmt.Errorf("删除用户组权限关联失败: %w", result.Error)
    }
    if result.RowsAffected == 0 {
        return fmt.Errorf("用户组权限关联不存在")
    }
    return nil
}
```

### 10. 实现BatchCreate方法

实现批量创建用户组权限关联的方法：

```go
// BatchCreate 批量创建用户组权限关联
func (r *userGroupPermissionRepository) BatchCreate(ctx context.Context, userGroupPermissions []*model.UserGroupPermission) error {
    if len(userGroupPermissions) == 0 {
        return nil
    }
    if err := r.db.WithContext(ctx).CreateInBatches(userGroupPermissions, 100).Error; err != nil {
        return fmt.Errorf("批量创建用户组权限关联失败: %w", err)
    }
    return nil
}
```

### 11. 实现BatchDelete方法

实现批量删除用户组权限关联的方法：

```go
// BatchDelete 批量删除用户组权限关联
func (r *userGroupPermissionRepository) BatchDelete(ctx context.Context, userGroupID int64, permissionIDs []int64) error {
    if len(permissionIDs) == 0 {
        return nil
    }
    result := r.db.WithContext(ctx).
        Where("user_group_id = ? AND permission_id IN ?", userGroupID, permissionIDs).
        Delete(&model.UserGroupPermission{})
    if result.Error != nil {
        return fmt.Errorf("批量删除用户组权限关联失败: %w", result.Error)
    }
    return nil
}
```

### 12. 代码规范要求

- 所有错误消息使用中文
- 使用context.Context传递上下文
- 使用GORM的WithContext方法
- 处理GORM错误，包括RecordNotFound错误
- 在Delete方法中检查RowsAffected
- 批量操作使用CreateInBatches提高性能
- 所有方法都有清晰的注释（使用中文）

## 验收标准

1. **接口定义完整**
   - UserGroupPermissionRepository接口包含所有必需的方法：Create、GetByUserGroupID、GetByPermissionID、GetByUserGroupIDAndPermissionID、Delete、DeleteByUserGroupIDAndPermissionID、BatchCreate、BatchDelete
   - 方法签名正确，参数和返回值类型正确
   - 接口注释完整，使用中文

2. **实现完整**
   - userGroupPermissionRepository结构体正确实现UserGroupPermissionRepository接口
   - 所有接口方法都有对应的实现
   - 使用GORM进行数据库操作
   - 使用context.Context传递上下文

3. **错误处理正确**
   - 所有错误都使用中文错误消息
   - 正确处理GORM的RecordNotFound错误
   - Delete方法检查RowsAffected
   - 错误消息清晰，包含上下文信息

4. **批量操作实现正确**
   - BatchCreate方法使用CreateInBatches进行批量创建
   - BatchDelete方法正确批量删除关联
   - 批量操作处理空列表情况

5. **代码规范符合要求**
   - 代码通过golangci-lint检查，无错误和警告
   - 代码注释完整，使用中文
   - 遵循Golang编码规范
   - 遵循项目代码组织结构

6. **功能正确性**
   - Create方法正确创建用户组权限关联
   - GetByUserGroupID方法正确查询用户组的权限列表
   - GetByPermissionID方法正确查询权限的用户组列表
   - GetByUserGroupIDAndPermissionID方法正确查询特定关联
   - Delete方法正确删除关联
   - DeleteByUserGroupIDAndPermissionID方法正确删除特定关联
   - BatchCreate方法正确批量创建关联
   - BatchDelete方法正确批量删除关联

## 相关文件

- `internal/pkg/repository/user_group_permission_repository.go` - 用户组权限关联仓储接口和实现
- `internal/pkg/model/user_group_permission.go` - 用户组权限关联模型定义
- `docs/TASKS/TASK004/TASK004-40.md` - 创建UserGroupPermission模型任务文档
- `docs/TASKS/TASK002/TASK002-09.md` - 用户组权限关联表数据库设计文档
- `.cursor/rules/golang.mdc` - Golang编码规范
- `docs/TASKS/TASK004/TASK004.md` - 主任务文档

## Git 提交信息任务

任务完成后，需要生成 Git 提交信息。请根据实际修改内容生成合适的提交信息。

---

**文档版本**: 1.1.0  
**最后更新**: 2026-01-03 15:00:00 CST

