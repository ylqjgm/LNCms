# TASK004-12: 编写UserGroupService单元测试

## 任务信息

**任务编号**: TASK004-12  
**父任务**: TASK004  
**任务名称**: 编写UserGroupService单元测试  
**版本信息**: v1.1.0  
**当前状态**: 计划中  
**创建时间**: 2026-01-03 14:54:20 CST

## 任务描述

编写用户组服务层单元测试。包括：1) 测试UserGroupService的所有方法；2) 测试输入验证功能；3) 使用Mock Repository进行测试；4) 确保测试覆盖率达标。所有测试用例描述和错误消息必须使用中文。

## 依赖任务

- TASK004-11: 创建UserGroupService接口和实现

## 实现指南

### 1. 设置测试环境

在 `internal/pkg/service/user_group_service_test.go` 文件中创建测试文件：

```go
package service

import (
    "context"
    "testing"
    
    "github.com/stretchr/testify/assert"
    "github.com/stretchr/testify/mock"
    "github.com/stretchr/testify/require"
    "yourproject/internal/pkg/model"
    "yourproject/internal/pkg/repository"
)

// MockUserGroupRepository Mock用户组仓储
type MockUserGroupRepository struct {
    mock.Mock
}

func (m *MockUserGroupRepository) GetByID(ctx context.Context, id int64) (*model.UserGroup, error) {
    args := m.Called(ctx, id)
    if args.Get(0) == nil {
        return nil, args.Error(1)
    }
    return args.Get(0).(*model.UserGroup), args.Error(1)
}

func (m *MockUserGroupRepository) GetAll(ctx context.Context) ([]*model.UserGroup, error) {
    args := m.Called(ctx)
    if args.Get(0) == nil {
        return nil, args.Error(1)
    }
    return args.Get(0).([]*model.UserGroup), args.Error(1)
}

func (m *MockUserGroupRepository) Create(ctx context.Context, userGroup *model.UserGroup) error {
    args := m.Called(ctx, userGroup)
    return args.Error(0)
}

func (m *MockUserGroupRepository) Update(ctx context.Context, userGroup *model.UserGroup) error {
    args := m.Called(ctx, userGroup)
    return args.Error(0)
}

func (m *MockUserGroupRepository) Delete(ctx context.Context, id int64) error {
    args := m.Called(ctx, id)
    return args.Error(0)
}

func (m *MockUserGroupRepository) GetByGroupName(ctx context.Context, groupName string) (*model.UserGroup, error) {
    args := m.Called(ctx, groupName)
    if args.Get(0) == nil {
        return nil, args.Error(1)
    }
    return args.Get(0).(*model.UserGroup), args.Error(1)
}
```

### 2. 测试GetByID方法

测试GetByID方法的正常情况和异常情况：

```go
func TestUserGroupService_GetByID(t *testing.T) {
    mockRepo := new(MockUserGroupRepository)
    service := NewUserGroupService(mockRepo)
    ctx := context.Background()
    
    tests := []struct {
        name    string
        id      int64
        setup   func()
        wantErr bool
        errMsg  string
    }{
        {
            name: "正常情况：获取存在的用户组",
            id:   1,
            setup: func() {
                mockRepo.On("GetByID", ctx, int64(1)).Return(&model.UserGroup{
                    ID:        1,
                    GroupName: "测试用户组",
                }, nil)
            },
            wantErr: false,
        },
        {
            name: "异常情况：ID无效",
            id:   0,
            setup: func() {
                // 不需要设置Mock，因为会在验证阶段失败
            },
            wantErr: true,
            errMsg:  "用户组ID无效",
        },
        {
            name: "异常情况：用户组不存在",
            id:   999,
            setup: func() {
                mockRepo.On("GetByID", ctx, int64(999)).Return(nil, fmt.Errorf("用户组不存在"))
            },
            wantErr: true,
            errMsg:  "获取用户组失败",
        },
    }
    
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            mockRepo.ExpectedCalls = nil
            mockRepo.Calls = nil
            if tt.setup != nil {
                tt.setup()
            }
            
            got, err := service.GetByID(ctx, tt.id)
            if tt.wantErr {
                assert.Error(t, err)
                if tt.errMsg != "" {
                    assert.Contains(t, err.Error(), tt.errMsg)
                }
                assert.Nil(t, got)
            } else {
                assert.NoError(t, err)
                assert.NotNil(t, got)
                assert.Equal(t, tt.id, got.ID)
            }
            mockRepo.AssertExpectations(t)
        })
    }
}
```

### 3. 测试GetAll方法

测试GetAll方法：

```go
func TestUserGroupService_GetAll(t *testing.T) {
    mockRepo := new(MockUserGroupRepository)
    service := NewUserGroupService(mockRepo)
    ctx := context.Background()
    
    tests := []struct {
        name    string
        setup   func()
        wantErr bool
    }{
        {
            name: "正常情况：获取所有用户组",
            setup: func() {
                mockRepo.On("GetAll", ctx).Return([]*model.UserGroup{
                    {ID: 1, GroupName: "用户组1"},
                    {ID: 2, GroupName: "用户组2"},
                }, nil)
            },
            wantErr: false,
        },
        {
            name: "异常情况：查询失败",
            setup: func() {
                mockRepo.On("GetAll", ctx).Return(nil, fmt.Errorf("数据库错误"))
            },
            wantErr: true,
        },
    }
    
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            mockRepo.ExpectedCalls = nil
            mockRepo.Calls = nil
            if tt.setup != nil {
                tt.setup()
            }
            
            got, err := service.GetAll(ctx)
            if tt.wantErr {
                assert.Error(t, err)
                assert.Nil(t, got)
            } else {
                assert.NoError(t, err)
                assert.NotNil(t, got)
                assert.Greater(t, len(got), 0)
            }
            mockRepo.AssertExpectations(t)
        })
    }
}
```

### 4. 测试Create方法

测试Create方法的正常情况和异常情况：

```go
func TestUserGroupService_Create(t *testing.T) {
    mockRepo := new(MockUserGroupRepository)
    service := NewUserGroupService(mockRepo)
    ctx := context.Background()
    
    tests := []struct {
        name    string
        req     *CreateUserGroupRequest
        setup   func()
        wantErr bool
        errMsg  string
    }{
        {
            name: "正常情况：创建用户组",
            req: &CreateUserGroupRequest{
                GroupName:        "新用户组",
                GroupDescription: stringPtr("新用户组描述"),
                IsActive:         true,
            },
            setup: func() {
                mockRepo.On("GetByGroupName", ctx, "新用户组").Return(nil, fmt.Errorf("用户组不存在"))
                mockRepo.On("Create", ctx, mock.AnythingOfType("*model.UserGroup")).Return(nil)
            },
            wantErr: false,
        },
        {
            name: "异常情况：请求参数为空",
            req:  nil,
            setup: func() {
                // 不需要设置Mock
            },
            wantErr: true,
            errMsg:  "请求参数不能为空",
        },
        {
            name: "异常情况：用户组名称为空",
            req: &CreateUserGroupRequest{
                GroupName: "",
            },
            setup: func() {
                // 不需要设置Mock
            },
            wantErr: true,
            errMsg:  "用户组名称不能为空",
        },
        {
            name: "异常情况：用户组名称已存在",
            req: &CreateUserGroupRequest{
                GroupName: "已存在的用户组",
            },
            setup: func() {
                mockRepo.On("GetByGroupName", ctx, "已存在的用户组").Return(&model.UserGroup{
                    ID:        1,
                    GroupName: "已存在的用户组",
                }, nil)
            },
            wantErr: true,
            errMsg:  "用户组名称已存在",
        },
    }
    
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            mockRepo.ExpectedCalls = nil
            mockRepo.Calls = nil
            if tt.setup != nil {
                tt.setup()
            }
            
            got, err := service.Create(ctx, tt.req)
            if tt.wantErr {
                assert.Error(t, err)
                if tt.errMsg != "" {
                    assert.Contains(t, err.Error(), tt.errMsg)
                }
                assert.Nil(t, got)
            } else {
                assert.NoError(t, err)
                assert.NotNil(t, got)
                assert.Equal(t, tt.req.GroupName, got.GroupName)
            }
            mockRepo.AssertExpectations(t)
        })
    }
}
```

### 5. 测试Update方法

测试Update方法：

```go
func TestUserGroupService_Update(t *testing.T) {
    mockRepo := new(MockUserGroupRepository)
    service := NewUserGroupService(mockRepo)
    ctx := context.Background()
    
    tests := []struct {
        name    string
        id      int64
        req     *UpdateUserGroupRequest
        setup   func()
        wantErr bool
        errMsg  string
    }{
        {
            name: "正常情况：更新用户组",
            id:   1,
            req: &UpdateUserGroupRequest{
                GroupName:        "更新后的用户组名",
                GroupDescription: stringPtr("更新后的描述"),
                IsActive:         false,
            },
            setup: func() {
                mockRepo.On("GetByID", ctx, int64(1)).Return(&model.UserGroup{
                    ID:        1,
                    GroupName: "原始用户组名",
                }, nil)
                mockRepo.On("GetByGroupName", ctx, "更新后的用户组名").Return(nil, fmt.Errorf("用户组不存在"))
                mockRepo.On("Update", ctx, mock.AnythingOfType("*model.UserGroup")).Return(nil)
            },
            wantErr: false,
        },
        {
            name: "异常情况：ID无效",
            id:   0,
            req: &UpdateUserGroupRequest{
                GroupName: "用户组名",
            },
            setup: func() {
                // 不需要设置Mock
            },
            wantErr: true,
            errMsg:  "用户组ID无效",
        },
        {
            name: "异常情况：用户组不存在",
            id:   999,
            req: &UpdateUserGroupRequest{
                GroupName: "用户组名",
            },
            setup: func() {
                mockRepo.On("GetByID", ctx, int64(999)).Return(nil, fmt.Errorf("用户组不存在"))
            },
            wantErr: true,
            errMsg:  "用户组不存在",
        },
        {
            name: "异常情况：用户组名称已存在",
            id:   1,
            req: &UpdateUserGroupRequest{
                GroupName: "已存在的用户组名",
            },
            setup: func() {
                mockRepo.On("GetByID", ctx, int64(1)).Return(&model.UserGroup{
                    ID:        1,
                    GroupName: "原始用户组名",
                }, nil)
                mockRepo.On("GetByGroupName", ctx, "已存在的用户组名").Return(&model.UserGroup{
                    ID:        2,
                    GroupName: "已存在的用户组名",
                }, nil)
            },
            wantErr: true,
            errMsg:  "用户组名称已存在",
        },
    }
    
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            mockRepo.ExpectedCalls = nil
            mockRepo.Calls = nil
            if tt.setup != nil {
                tt.setup()
            }
            
            got, err := service.Update(ctx, tt.id, tt.req)
            if tt.wantErr {
                assert.Error(t, err)
                if tt.errMsg != "" {
                    assert.Contains(t, err.Error(), tt.errMsg)
                }
                assert.Nil(t, got)
            } else {
                assert.NoError(t, err)
                assert.NotNil(t, got)
                assert.Equal(t, tt.req.GroupName, got.GroupName)
            }
            mockRepo.AssertExpectations(t)
        })
    }
}
```

### 6. 测试Delete方法

测试Delete方法：

```go
func TestUserGroupService_Delete(t *testing.T) {
    mockRepo := new(MockUserGroupRepository)
    service := NewUserGroupService(mockRepo)
    ctx := context.Background()
    
    tests := []struct {
        name    string
        id      int64
        setup   func()
        wantErr bool
        errMsg  string
    }{
        {
            name: "正常情况：删除用户组",
            id:   1,
            setup: func() {
                mockRepo.On("GetByID", ctx, int64(1)).Return(&model.UserGroup{
                    ID:        1,
                    GroupName: "待删除用户组",
                }, nil)
                mockRepo.On("Delete", ctx, int64(1)).Return(nil)
            },
            wantErr: false,
        },
        {
            name: "异常情况：ID无效",
            id:   0,
            setup: func() {
                // 不需要设置Mock
            },
            wantErr: true,
            errMsg:  "用户组ID无效",
        },
        {
            name: "异常情况：用户组不存在",
            id:   999,
            setup: func() {
                mockRepo.On("GetByID", ctx, int64(999)).Return(nil, fmt.Errorf("用户组不存在"))
            },
            wantErr: true,
            errMsg:  "用户组不存在",
        },
    }
    
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            mockRepo.ExpectedCalls = nil
            mockRepo.Calls = nil
            if tt.setup != nil {
                tt.setup()
            }
            
            err := service.Delete(ctx, tt.id)
            if tt.wantErr {
                assert.Error(t, err)
                if tt.errMsg != "" {
                    assert.Contains(t, err.Error(), tt.errMsg)
                }
            } else {
                assert.NoError(t, err)
            }
            mockRepo.AssertExpectations(t)
        })
    }
}

// stringPtr 辅助函数：将字符串转换为指针
func stringPtr(s string) *string {
    return &s
}
```

### 7. 运行测试和覆盖率检查

```bash
# 运行测试
go test ./internal/pkg/service -v -run TestUserGroupService

# 生成覆盖率报告
go test ./internal/pkg/service -coverprofile=coverage.out -run TestUserGroupService

# 查看覆盖率报告
go tool cover -html=coverage.out

# 检查覆盖率是否达标（应达到80%以上）
go tool cover -func=coverage.out | grep user_group_service
```

## 验收标准

1. **测试覆盖完整**
   - 测试覆盖UserGroupService的所有方法：GetByID、GetAll、Create、Update、Delete
   - 每个方法都测试了正常情况和异常情况
   - 测试覆盖输入验证功能

2. **测试用例质量**
   - 使用表驱动测试（table-driven tests）处理多个测试场景
   - 测试用例名称清晰描述测试场景（使用中文）
   - 测试用例独立，不依赖执行顺序
   - 使用Mock Repository进行测试

3. **Mock使用正确**
   - 正确创建Mock Repository
   - 正确设置Mock期望
   - 正确验证Mock调用
   - 每个测试后清理Mock状态

4. **断言和验证**
   - 使用testify/assert和testify/require进行断言
   - 验证返回值正确性
   - 验证错误消息正确性（使用中文）
   - 验证Mock调用次数和参数

5. **代码覆盖率达标**
   - 测试覆盖率应达到80%以上
   - 关键代码路径都有测试覆盖
   - 错误处理路径都有测试覆盖

6. **代码规范符合要求**
   - 测试代码通过golangci-lint检查，无错误和警告
   - 测试用例描述和错误消息使用中文
   - 遵循Golang单元测试规范
   - 测试代码清晰、易读、易维护

## 相关文件

- `internal/pkg/service/user_group_service_test.go` - 用户组服务单元测试文件
- `internal/pkg/service/user_group_service.go` - 用户组服务接口和实现
- `docs/TASKS/TASK004/TASK004-11.md` - 创建UserGroupService接口任务文档
- `.cursor/rules/testing.mdc` - 单元测试规范
- `docs/TASKS/TASK004/TASK004.md` - 主任务文档

## Git 提交信息任务

任务完成后，需要生成 Git 提交信息。请根据实际修改内容生成合适的提交信息。

---

**文档版本**: 1.1.0  
**最后更新**: 2026-01-03 14:54:20 CST

