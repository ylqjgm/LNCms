# TASK002-04: 数据库设计 - 核心模块 - 等级表

## 任务信息

**任务编号**: TASK002-04  
**父任务**: TASK002  
**任务名称**: 数据库设计 - 核心模块 - 等级表  
**版本信息**: v1.0.0  
**当前状态**: 计划中  
**创建时间**: 2026-01-02 22:13:54 CST

## 任务描述

设计核心模块的等级表（levels），用于存储网站前台用户等级信息。必须遵循 PostgreSQL 数据库设计规范，包含完整的字段定义、约束、索引、注释等。

## 依赖任务

无

## 实现指南

### 1. 分析需求文档

阅读 `docs/Requirements/Core.md` 文档，了解等级管理的详细需求：
- 等级管理：等级名称、等级描述、等级达成条件（积分、货币、充值数、消费数、评论数、阅读数等可选参数）
- 等级用于前台用户等级管理，支持按条件自动升级

### 2. 设计等级表结构

设计 `levels` 表，包含等级基本信息：

```sql
CREATE TABLE levels (
    id BIGSERIAL PRIMARY KEY,
    level_name VARCHAR(100) NOT NULL UNIQUE,
    level_description TEXT,
    level_conditions JSONB,
    level_order INTEGER NOT NULL,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
);

-- 创建索引
CREATE INDEX idx_levels_level_name ON levels(level_name);
CREATE INDEX idx_levels_level_order ON levels(level_order);
CREATE INDEX idx_levels_is_active ON levels(is_active);
CREATE UNIQUE INDEX uk_levels_level_name ON levels(level_name);
CREATE UNIQUE INDEX uk_levels_level_order ON levels(level_order);

-- 创建触发器自动更新 updated_at
CREATE OR REPLACE FUNCTION update_levels_updated_at()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = CURRENT_TIMESTAMP;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER update_levels_updated_at
    BEFORE UPDATE ON levels
    FOR EACH ROW
    EXECUTE FUNCTION update_levels_updated_at();

-- 添加表注释
COMMENT ON TABLE levels IS '等级表，存储网站前台用户等级信息';
COMMENT ON COLUMN levels.id IS '等级ID，自增主键';
COMMENT ON COLUMN levels.level_name IS '等级名称，唯一标识符';
COMMENT ON COLUMN levels.level_description IS '等级描述，说明等级的用途和特权';
COMMENT ON COLUMN levels.level_conditions IS '等级达成条件，JSONB格式，包含积分、货币、充值数、消费数、评论数、阅读数等可选参数';
COMMENT ON COLUMN levels.level_order IS '等级排序，数字越小等级越高，用于排序和比较';
COMMENT ON COLUMN levels.is_active IS '是否启用，TRUE-启用，FALSE-禁用';
COMMENT ON COLUMN levels.created_at IS '创建时间，自动设置为当前时间';
COMMENT ON COLUMN levels.updated_at IS '更新时间，每次更新时自动更新';
```

### 3. 设计约束

- **主键约束**：`pk_levels` (id)
- **唯一约束**：`uk_levels_level_name` (level_name)、`uk_levels_level_order` (level_order)
- **非空约束**：level_name、level_order 必须非空
- **默认值**：is_active 默认为 TRUE

### 4. 设计索引

- **主键索引**：`pk_levels` (id) - 自动创建
- **唯一索引**：`uk_levels_level_name` (level_name) - 确保等级名称唯一
- **唯一索引**：`uk_levels_level_order` (level_order) - 确保等级排序唯一
- **普通索引**：`idx_levels_level_name` (level_name) - 用于快速查询
- **普通索引**：`idx_levels_level_order` (level_order) - 用于排序查询
- **普通索引**：`idx_levels_is_active` (is_active) - 用于筛选启用的等级

### 5. 考虑性能优化

- 为 level_name 和 level_order 创建唯一索引，支持快速查询和唯一性检查
- 为 is_active 创建索引，支持状态筛选查询
- 使用触发器自动更新 updated_at 字段

## 验收标准

1. 表结构设计完整，包含所有必要字段
2. 所有字段都有适当的类型、约束和默认值
3. 索引设计合理，支持常用查询场景
4. 表注释和字段注释完整，使用中文
5. 触发器正确创建，自动更新 updated_at 字段
6. 表设计符合 PostgreSQL 数据库设计规范

## 相关文件

- `docs/Requirements/Core.md` - 核心模块需求文档
- `.cursor/rules/database.mdc` - PostgreSQL 数据库设计规范
- `docs/TASKS/TASK002/TASK002.md` - 主任务文档

## Git 提交信息任务

任务完成后，需要生成 Git 提交信息。请根据实际修改内容生成合适的提交信息。

---

**文档版本**: 1.0  
**最后更新**: 2026-01-02 22:13:54 CST
